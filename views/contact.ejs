<%- include('layout/header') %> 
        <div class="card text-left w-30">
          <div class="card-header text-center">
            <h5>List of Contacts</h5>
          </div>
          <% if (data.length < 1) { %>
            <p>Contact data is empty</p>
            <div class="card-header">
              <a href="/contact/add" class="btn btn-primary" role="button" aria-disabled="true">Add Contact</a>
            </div>
            <% } else {%>
          <div class="card-header">
            <a href="/contact/add" class="btn btn-primary" role="button" aria-disabled="true">Add Contact</a>
          </div>
          <div class="card-body">
            <table class="table table-success table-striped">
              <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Number</th>
                <th>Action</th>
              </tr>
              <% data.forEach(contacts => { %>
                <tr>
                  <td><%= contacts.name %> </td>
                  <td><%= contacts.email %> </td>
                  <td><%= contacts.number %> </td>

                  <td>           
                    <a href="/contact/<%= contacts.id %>/edit" class="btn btn-primary" role="button" aria-disabled="true">Edit</a>
                    <a href="/contact/<%= contacts.id %>/delete" class="btn btn-primary" role="button" aria-disabled="true">Delete</a>

                  </td>
              </tr>
              <% }) %>
          </table>
          </div>
        </div>

      <% } %>
<%- include('layout/footer') %> 
